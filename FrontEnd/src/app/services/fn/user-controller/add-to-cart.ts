/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface AddToCart$Params {
  idItem: number;
  qnt: number;
  idBac: number;
}

export function addToCart(http: HttpClient, rootUrl: string, params: AddToCart$Params, context?: HttpContext): Observable<StrictHttpResponse<{
}>> {
  const rb = new RequestBuilder(rootUrl, addToCart.PATH, 'post');
  if (params) {
    rb.query('idItem', params.idItem, {});
    rb.query('qnt', params.qnt, {});
    rb.query('idBac', params.idBac, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      }>;
    })
  );
}

addToCart.PATH = '/user/addToCart';
